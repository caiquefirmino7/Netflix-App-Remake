<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.NetflixAppRemake" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorSecondary">@color/red</item>
    </style>

    <style name="Theme.NetflixAppRemake" parent="Base.Theme.NetflixAppRemake">
        <!-- Definindo a cor da barra de status como preto -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

</resources>
